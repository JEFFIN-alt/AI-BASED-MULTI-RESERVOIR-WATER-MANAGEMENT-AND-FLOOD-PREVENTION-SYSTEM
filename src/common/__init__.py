"""AquaFlow shared, algorithm-agnostic utilities.

Contents
--------
units
    The canonical unit and state contract for the whole project, plus the
    single explicit conversion boundary between the EXTERNAL (percent) and
    INTERNAL (fraction) representations of a reservoir gate position.
"""
